//File for each reducer and then export that reducer 
//This reducer is for users so is named users.js
export default (state = [], action) => {
  switch (action.type) {
    default: 
      return state
  }
}